{
  "name": "jsb-hospitality-site",
  "version": "1.0.0",
  "private": true,
  "description": "JSB Hospitality direct-booking site (static pages + Vercel serverless API, zero npm dependencies).",
  "engines": {
    "node": ">=18"
  }
}
